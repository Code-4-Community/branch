# Read at build time. Without it, src/lib/api.ts falls back to localhost ports.
NEXT_PUBLIC_API_BASE_URL=

# Set only by the preview-env workflow. Leave empty locally and in production.
PREVIEW_BASE_PATH=
